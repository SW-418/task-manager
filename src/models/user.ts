export class User {
    firstName: string
    surname: string
    age: number

    constructor(firstName: string, surname: string, age: number) {
        this.firstName = firstName;
        this.surname = surname;
        this.age = age;
    }
}
